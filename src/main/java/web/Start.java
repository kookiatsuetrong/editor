package web;
import org.springframework.boot.*;
import org.springframework.boot.autoconfigure.*;
@SpringBootApplication
public class Start {
    public static void main(String[] z) {
        SpringApplication.run(Start.class, z);
    }
}
